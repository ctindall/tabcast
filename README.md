#tabcast

a tool to turn, ugly messy podcast XML into lines of tab-seperated values that are easy to work with on the command line



Installation:

1. install ruby
2. copy "tabcast" file into your $PATH

Usage:

`tabcast <feed url>`
